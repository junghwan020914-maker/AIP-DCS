# ryujan 팀 공유 노트

작성 2026-08-10. 우리 쪽 검증에서 나온 것 중 **상대 팀에도 그대로 해당되는 사항**만 추렸다.
(1)(2)는 대회 결과에 직접 영향이 갈 수 있어 우선순위가 높다.

---

## 1. 🔴 파일명 충돌 — `Rule_mine.xml`

우리와 ryujan 레포가 **같은 파일명 `Rule_mine.xml`을 쓴다.** `CPPBehaviorTree.cpp`에서

```cpp
tree = Factory.createTreeFromFile("./Rule_mine.xml");
```

처럼 **상대경로로 하드코딩**되어 있어, 두 DLL을 같은 작업디렉터리에 놓으면
**둘 다 먼저 발견된 하나의 XML을 읽는다.** 즉 한쪽 기체가 상대의 트리로 싸운다.

- 로컬 대전 테스트에서 조용히 잘못된 결과가 나온다(에러가 아니라 오작동이라 눈치채기 어렵다).
- 우리는 변형 DLL마다 XML 파일명을 따로 두는 방식으로 회피 중이다.
- 제출 시 서버가 어떤 디렉터리 레이아웃을 쓰는지에 따라 **본선에서도 문제가 될 수 있다.**

권장: XML 파일명을 팀/버전별로 유일하게 하거나, DLL 자기 경로 기준으로 로드.

## 2. 🔴 v32 실서버 경로 좌표 이상 + 매 틱 stdout 출력

`AIP_v32.dll`을 서버 경로(`BuildPlaneData` + `StepWithPlaneData`)로 돌리면
거리 계산이 터진다:

```
Dist=4.67894e+07
```

4.7천만 m다. 로컬(직접 FDM) 경로에서는 정상이므로 **서버 경로에서만 나타나는 좌표 변환
문제**로 보인다. 실전에서 이 값이면 모든 거리 게이트가 오작동한다.

같은 DLL이 **매 틱 stdout에 출력**한다:

```
[ROOT] team=2 status=2 Z=2340.64 VP=(2124.01,-44.2313,4559.64)
[DECO_Dist] team=2 UD=Less Cur=5097.98 Input=1100
[SelectTarget] team=2 EnemyN=1 TgtZ=4376.26
[ACTIVE] [RED] LeadPredict dist=5357.69 dV=47.7915 thr=1 om=0.202241 lt=3
```

60Hz면 틱 예산이 16.67ms인데 동기 stdout은 그 안에서 무시할 수 없는 비용이다.
우리도 같은 문제가 있어 전부 `#ifdef` 가드로 묶었다(`MINE_DBG_TRACE`,
`THREAT_DBG_TRACE`, `WOBBLE_DBG_TRACE` 등). 제출 전 확인 권장.

## 2-1. 🔴 **템플릿 예외 처리가 오히려 패배를 부른다** (모든 팀 해당 가능)

`CPPBehaviorTree::RunCPPBT`의 예외 처리가 원본 그대로면 이렇게 되어 있다:

```cpp
catch (const std::exception& e)
{
    //...일단 최대한 안전하게 행동하도록 설정
    VP = Vector3(0,0,0);
    Throttle = 1.0f;
    std::cout << "ERROR!!!!!!!!! ..." << std::endl;
    std::cout << "Temp Result VP : (0,0,0), Throttle : 1" << std::endl;
    throw;                                   // <- 여기
}
```

세 가지가 문제다:

1. **`VP = (0,0,0)`은 "안전"이 아니다.** 좌표 원점을 조준하라는 뜻이라, 자기 위치에
   따라 **지면으로 내리꽂는 명령**이 된다. 규정 제6조1항상 추락은 **즉시 패배**다.
2. **마지막 `throw;` 때문에 위에서 설정한 안전값이 실제로 쓰이지 않는다.**
   예외가 `Step()` 밖으로 전파된다. 로컬 하네스에서는 `OSError: BT Step failed`로
   나타나고(우리도 겪었다), 서버에서는 기권·중단으로 이어질 수 있다.
   **주석과 코드가 정면으로 모순된다.**
3. 예외가 매 틱 나면 stdout 2줄씩 60Hz로 쏟는다.

우리 수정: 수평 기수 방향 +2000m, 고도 +200m(완만 상승 직진) / Throttle 0.9 /
**재던지기 제거** / 로그는 static 플래그로 최초 1회만.
지면에서 멀어지는 방향이라 최악의 경우에도 추락을 부르지 않는다.

**또 우리도 2번(무가드 stdout)을 갖고 있었다.** `SelectTarget`이 적 리스트가 비면
매 틱 `"Target is not Valid!!!!!!"`를 쓴다 — 원본 그대로다. `#ifdef`로 묶었다.
남을 지적하기 전에 우리 것부터 확인하는 게 맞았다.

## 3. 제어기(`Controller_CY`) 열화 3건 — 원본 공통

주최측 제공 `Controller_CY`는 "예시 및 저점 방어용"이고 필수가 아니지만,
쓰고 있다면 아래 3건은 명백한 열화라 그대로 이득이다. 우리 실측은 공식조건
40시드 vs v32, 채점단위(HP/판) 기준이다.

### (a) 러더 이동평균의 정수 절삭 — **단독 +7.3%**

```cpp
int MFsum = 0;                       // ❌ MF[]는 [-6,6] float
for (int i = 0; i < 20; i++) MFsum += MF[i];
RudderCMD = (MFsum / 20 + RudderCMD) / 2;   // ❌ 정수 나눗셈
```

`|MFsum| < 20`이면 결과가 통째로 0이라, **이동평균 필터가 아니라 "러더 게인 50%
감쇠기"로만 동작한다.** LOS가 작은 정밀조준 구간에서 특히 그렇다.
큰 각도에서는 정수 계단이 불연속 점프를 만든다. `float` 누산으로 교체하면 된다.

### (b) 롤 좌우 비대칭 — **단독 +1.6%, `<1도` 시간을 처음 움직인 변경**

```cpp
if (RollCMD < 0.1) RollCMD *= 3;     // ❌ 음수 롤 전체(-1~0)가 항상 3배
```

부호 조건이라 **음수 방향만 3배**다. 좌우 게인이 3배 차이나 한쪽만 조기 포화하고,
정밀조준 구간에서 조준점 주위 리밋사이클로 나타난다.
`RollCMD *= 3`(무조건)으로 대칭화하니 `<1도` 시간 1.27 → 1.39s(+9.4%),
`<3도` 5.58 → 9.05s, 승패 36/3 → 38/1.

### (c) 코사인 Roll_Effect + D항 강화 — **반드시 짝으로, 합쳐서 +12.3%**

```cpp
float Roll_Effect = clamp(std::cos(UTAngle), 0, 1);   // 원본은 1 - |UT|/90 (선형)
```

와 피치 D항(`Kd=0.02`, 게이트 `LOS<8도`, 클램프 ±0.25)을 **함께** 넣어야 한다.

| 조합 | 순이득 | 승/패 |
|---|---|---|
| 선형 + D약(.01/5도/.15) | +0.7474 | 38/2 |
| 코사인 + D약 | +0.7096 | 37/3 (−5.1%) |
| 선형 + D강(.02/8도/.25) | +0.7107 | 37/3 (−4.9%) |
| **코사인 + D강** | **+0.8391** | **40/0** (+12.3%) |

각각 단독으로는 −5%인데 합치면 +12.3%다. 코사인이 만드는 진동을 강화된 D가 잡아준다.
**한쪽만 되돌리면 반드시 회귀한다.**

## 3-1. 🔴 추가 발견 — `ERROR_Effect`의 "I항"은 적분기가 아니다 (상시 당김)

```cpp
float ERROR_Effect = clamp(LOS / 6 + clamp(GetLOSErrorSUM(LOS) / 7.5, 0, 0.25) + dTerm, 0, 1.5);
```

가운데 항은 **최근 60샘플의 평균**이지 적분이 아니다. 부호에 반응하지 못하므로
정상 추격에서 `평균 2.5 / 7.5 = 0.33 -> 0.25로 영구 포화`한다.
LOS 3도면 P(0.5) + I(0.25) = **0.75, 반스틱 당김이 상시 걸린 상태**다.
400m/s에서 이건 큰 G이고 유도항력으로 에너지를 계속 갉아먹는다.

실측(우리 기체): **전추력 수평비행 최고속도 505m/s인데 추격 중 실속도는 421m/s** —
에어프레임 성능의 17%를 조준 진동에 버리고 있었다. 그래서 0.70 스로틀로 직진하는
표적(407m/s)조차 6시드 중 4판에서 200초 내내 못 잡았다.

**주의: I항을 그냥 없애면 더 나빠진다.** 상한을 0.25 -> 0으로 내렸더니 조준이 무너져
득점이 오히려 줄었다(0.1535 -> 0.0053). 이 항은 부하를 지고 있다.

우리 처방은 제어기가 아니라 **트리**에서 우회했다 — 사거리 밖(1300m 초과)이고 이미
정렬돼 있으면(LOS<6도) VP를 기수 쪽으로 15%만 남기고 당겨 상시 당김을 끊는다.
직진표적 무득점 4/6 -> **0/6**. 혼합비에 봉우리가 있다: 0.00은 조준을 아예 놓아
득점하던 판을 죽이고(2/6), 0.30은 당김이 되살아나 가속을 잃는다(1/6). **0.15가 최적.**

## 4. 참고 — 우리 쪽에서 기각된 것들 (중복 삽질 방지)

시도했다가 **명확히 악화**해서 되돌린 것들이다. 조건이 다르면 결과도 다를 수 있으나
참고는 될 것이다.

- **보어사이트 클램프 완화**: 75°(기준) > 100° > 120°. 90°도 −14%.
  클램프는 우회책이 아니라 **제어기를 자기가 잘 동작하는 영역에 붙잡아두는 장치**다.
- **거리 목표대를 200~300m로 당기기**: 데미지 계수는 오르지만 요구 각속도가 더 빨리
  올라 조준유지시간이 계수보다 빠르게 무너진다. 승 −3.
- **피치 D항 게이트 확대**(8° → 20°): v32 27승 → 26승, v29 24승 → 22승.
- **`LOS≥90` 피치 불연속 제거**(70~110° 선형 블렌드): 계단을 없앴는데도 악화.
  원인이 불연속이 아니라 큰 각도에서 `Roll_Effect`가 0으로 죽는 구조라서다.
- **`|UT|>90`에서 최대 롤**: `sin(UT)`가 180°에서 0이 되는 게 언뜻 버그로 보이지만,
  UT는 투영각이라 "표적이 기수보다 0.5° 아래"일 때도 180°가 된다. 최대 롤을 때리면
  사소한 오차에 180° 롤을 하고 넘어가면 반대쪽에서 또 구른다. 감쇠가 맞다.

## 4-1. 검증 방법 — 자작 "행동 원형" 상대를 권한다

우리는 상대 풀에 실존 교리 기반 원형을 직접 만들어 넣었다:
arcA(앵글) / arcE(에너지) / arcD(순수 방어) / arcV(수직).

**값을 했다.** 어떤 변경이 ryujan v29/v32와 arcA에서 전부 동률이라 통과할 뻔했는데,
arcE 하나가 **승점 -14.5** 붕괴를 잡아냈다(우리가 하강하는 동안 상대가 상승해
30판 전부 교전 성립 실패). 직전 세대 자기 자신(prev)도 회귀 감시자로 유효했다.

특히 **arcV(수직 파이터)가 우리 최악의 매치업**이다(승점 6.0/10). 트리에 수직 대응
분기가 없으면 같은 취약점이 있을 가능성이 높다 — One/TwoCircle은 **수평** 선회 선택이다.
`Task_ClimbOut`과 하강 노드만 있으면 새 노드 없이 원형을 세울 수 있다.

## 5. 우리 쪽 미해결 (같이 보면 좋을 것)

- **직진 표적 추격 문제는 3-1로 옮겼다** — 원인(I항 상시 포화)과 처방까지 나왔다.
- **수직 파이터에 답이 없다.** 자작 수직 원형(arcV) 상대 승점 6.0/10으로 전 상대 중
  최악이다. 10판 전부 중립 98%로 교전 자체가 성립하지 않는다. 위 트리 우회책(3-1)도
  여기엔 전이되지 않았다(6.0 -> 5.0). **duck(직진 도주)과 arcV(수직 도주)는 다른
  문제다** — 전자는 폐쇄, 후자는 수직 대응 부재. 해법 미상.
- **`UT = ±180°` 트랩**: 그 구간에서 `Roll_Effect = clamp(cos(UT),0,1) = 0`이라
  **피치 명령이 정확히 0**이고, 롤도 `sin(180°) = 0`이라 사실상 아무것도 못 한다.
  계측하면 수십 초씩 여기 머문다. 단순 강화는 위 4번처럼 역효과였다. 해법 미상.
