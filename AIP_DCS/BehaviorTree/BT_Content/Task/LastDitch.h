#pragma once
/*
	LastDitch (최후수단 감속 브레이크) Task 노드 — 뒤를 잡힌 상태를 끊는다.

	배경(08-13). 실서버 대전과 로컬 계측이 같은 결론을 가리켰다.

	  · v42 상대 30판: **수세 60,097 샘플 / 공세 912 샘플.** 교전 시간의 0.76%만
	    우리가 뒤에 있다. 조준을 못 하는 게 아니라 **기회 자체가 없다.**
	    (30판 중 27판 무득점, 3승 8패 19무)
	  · 실서버 396초 판도 같은 양상 — 양쪽 다 사격각 근처도 못 가고 무득점 무승부.
	    t=150초부터 상대가 우리 뒤를 잡고 끝까지 놓지 않았다.

	선회로는 이 비율을 못 바꾼다. 계측이 그걸 확정했다:
	  · 우리는 **받음각 제한이 아니라 G 제한**이다(받음각 p99 16.7도, 8G 초과 38.8%).
	  · 이미 **코너속도**에 있다 — 이 고도대에서 선회율 최대점이 KTAS 260~280이고
	    우리 교전 중앙이 282.6이다. 느려지면 오히려 나빠진다(180~200에서 14도/s).
	  · 최대 선회율 약 18.4도/s. **양쪽 기체가 동일한 F-16**이라 대칭이다.
	  → 성능이 같은 두 기체가 둘 다 최대로 돌면 한 원을 같이 도는 교착이 필연이다.

	그래서 남은 수단은 **에너지 비대칭을 스스로 만드는 것**뿐이다. 감속해서 상대를
	앞으로 흘려보내는 정통 최후수단(last-ditch)이다.

	`_decel_probe.py` 실측 — 스로틀 0 + 최대 당김:
	      유지    속도 손실    25초 뒤 복귀   최대받음각   판정
	      1.0s     -4.8 m/s      360.1        13.4도      회복
	      2.0s    -16.1          325.6        14.9도      회복
	      3.0s    -28.4          282.9        15.4도      회복
	      4.0s    -41.5          238.2        15.8도      회복
	      6.0s    -68.8          163.9        16.7도      위험
	      8.0s    -95.8          114.4        17.5도      **회복 실패**

	세 가지가 확정됐다:
	  (1) 짧은 펄스에서는 딥스톨이 안 난다. 최대 받음각 13~17도로 공력표(45도까지)
	      유효범위 안이다. 20초를 계속 당기면 63.9도까지 가지만 그건 이탈이다.
	  (2) **고도는 오히려 오른다**(2910 -> 3576m). 당기는 기동이라 줌 상승이 된다.
	      규정 제6조1항(고도 300m 이하 즉시 패배)을 건드리지 않는다.
	  (3) 진짜 비용은 **회복 시간 25초**다. 오버슛이 안 나면 25초짜리 취약 구간을
	      스스로 만든다. 코브라의 위험이 정확히 여기다.

	⚠️ ryujan v42 배포 노트의 경고를 설계에 반영했다:
	  · "근거리 6시 위협에서 **이탈**은 선회진입보다 나쁘다" — 그들이 `Task_Evade`
	    (횡 브레이크 = 등을 내주는 기동)를 제거해 90판 71/2/17 -> 77/5/8을 얻었다.
	    이 노드는 **등을 돌리지 않는다.** 상대 쪽으로 계속 당기면서 감속만 한다.
	  · "게이트를 좁히는 건 답이 아니다" — 1100->400으로 좁혔더니 더 늦게·더 가까이서
	    열려 오히려 나빴다. 그래서 여기서는 **게이트를 좁히는 대신 지속시간과
	    쿨다운으로 노출을 제한**한다.

	포트:
	  TargetAtaBelow  상대 ATA가 이 값 미만이어야 발동(상대가 내 뒤를 겨누고 있다)
	  MyAtaAbove      내 ATA가 이 값 초과여야 발동(내가 상대를 못 겨누고 있다 = 수세)
	  DistBelow       이 거리(m) 안이어야 발동. 멀면 오버슛이 안 나고 손해만 본다
	  ArmSeconds      위 조건이 이만큼 연속 유지돼야 발동(순간 스침 방지)
	  HoldSeconds     발동 지속시간. 실측 안전 상한 4초, 권장 3초
	  CooldownSeconds 해제 후 재발동 금지 시간. 실측 회복 시간 25초
	  FloorAlt        이 고도(m) 아래면 발동 안 함(줌 상승이라 여유가 있지만 이중 안전)
	  Throttle        발동 중 스로틀(0 = 아이들)

	상태는 전부 노드 멤버라 **라운드 경계에서 리셋**해야 한다. `BreakTurn`과 같은
	방식으로 위치 점프와 시간 역행을 둘 다 본다.
*/

#include "../../CPPBehaviorTree.h"

namespace Action
{
	class LastDitch : public SyncActionNode
	{
	public:
		LastDitch(const std::string& name, const NodeConfiguration& config)
			: SyncActionNode(name, config) {}

		static PortsList providedPorts();
		NodeStatus tick() override;

	private:
		double ArmedSince = -1.0;      // 발동 조건이 연속 유지되기 시작한 시각
		double FireStart = -1.0;       // 현재 발동 시작 시각 (-1 = 미발동)
		double LastFireEnd = -1e9;     // 마지막 해제 시각 (쿨다운 기준)
		double LastTickTime = -1e9;
		Vector3 LastKnownLocation;
		bool HasLastLocation = false;
	};
}
